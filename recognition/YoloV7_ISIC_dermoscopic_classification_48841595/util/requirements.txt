# Usage: pip install -r requirements.txt

# Base ----------------------------------------
matplotlib>=3.2.2
numpy>=1.18.5,<1.24.0
opencv-python>=4.1.1
Pillow>=7.1.2
PyYAML>=5.3.1
requests>=2.23.0
scipy>=1.4.1
torch>=1.7.0,!=1.12.0
torchvision>=0.8.1,!=0.13.0
tqdm>=4.41.0
protobuf<4.21.3

# Logging -------------------------------------
tensorboard>=2.4.1
# wandb

# Plotting ------------------------------------
pandas>=1.1.4
seaborn>=0.11.0

# Export --------------------------------------
# coremltools>=4.1  # CoreML export
# onnx>=1.9.0  # ONNX export
# onnx-simplifier>=0.3.6  # ONNX simplifier
# scikit-learn==0.19.2  # CoreML quantization
# tensorflow>=2.4.1  # TFLite export
# tensorflowjs>=3.9.0  # TF.js export
# openvino-dev  # OpenVINO export

# Extras --------------------------------------
ipython  # interactive notebook
psutil  # system utilization
thop  # FLOPs computation
# albumentations>=1.0.3
# pycocotools>=2.0  # COCO mAP
# roboflow

#inference ---------------

onnxruntime~=1.18.0; sys_platform == 'darwin'  # ONNX Runtime for macOS
# INSTALL ONNX RUNTIME GPU (CUDA 11.X)
onnxruntime-gpu~=1.18.0; sys_platform == 'linux' and platform_machine != 'arm64' and platform_machine != 'aarch64'
# INSTALL ONNX RUNTIME GPU (CUDA 12.X)
# pip onnxruntime-gpu --extra-index-url https://aiinfra.pkgs.visualstudio.com/PublicPackages/_packaging/onnxruntime-cuda-12/pypi/simple/
# Note: Refer to: https://onnxruntime.ai/docs/install/#python-installs
# Note: onnxruntime-gpu will be installed as the pre-built version on Jetson Nano
opencv-python~=4.8.1; sys_platform == 'darwin' or (sys_platform == 'linux' and platform_machine != 'arm64' and platform_machine != 'aarch64')
pydantic~=2.10.6

# Visualization
wasabi~=1.1.2  # Minimalist output messages library




